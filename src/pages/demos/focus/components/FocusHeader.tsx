import { SimsPageHeader } from '@/pages/sims/components/SimsPageHeader';

export function FocusHeader() {
  return (
    <SimsPageHeader
      title="FocusMode"
      description="A distraction-free chrome that paints over the viewport. Sidebar + Topbar are hidden; Escape exits. Useful for full-screen forms, document editors, and presentation surfaces."
    />
  );
}
